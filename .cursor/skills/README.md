# Skills: Protocol (Cursor)

Ba skill **project-local** khớp [`! Prompt to reload project information.txt`](../../!%20Prompt%20to%20reload%20project%20information.txt).

| Gọi ngắn (user) | Thư mục skill | `name` (metadata) |
|-----------------|----------------|-------------------|
| **Reload** | `protocol-reload` | `protocol-reload` |
| **Handover** | `protocol-handover` | `protocol-handover` |
| **Backup** | `protocol-backup` | `protocol-backup` |

**Cách dùng:** trong chat, nhập một từ kích hoạt (ví dụ `Reload` hoặc `Chạy skill Reload`) — Cursor gợi ý skill theo trường `description` trong `SKILL.md`. Bật / thêm thư mục `.cursor/skills/` vào cài đặt Agent Skills nếu IDE yêu cầu.

Bản cho Antigravity: `.agent/skills/` (cùng nội dung).
